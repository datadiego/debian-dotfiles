#
# Atajos independientes del WM
#

# Terminal
super + Return
	alacritty

# launcher de aplicaciones
super + d
	rofi -modi drun,run -show drun

# Aplicaciones con shortcuts
super + a
	gtk-launch chatgpt

super + b
	chromium

# Recargar configuración de sxhkd
super + Escape
	pkill -USR1 -x sxhkd

# Screenshots
Print
	flameshot gui

super + p
	flameshot gui

# Recargar polybar
super + shift + p
	killall polybar && polybar example &

#
# Atajos de bspwm
#

# Salir o reiniciar bspwm
super + alt + {q,r}
	bspc {quit,wm -r}

# Logout de bspwm
super + shift + e
	bspc quit

# Cerrar y matar ventanas
super + {_,shift + }w
	bspc node -{c,k}

# Cambiar entre los modos de distribución de ventanas
super + m
	bspc desktop -l next

# Intercambiar nodo actual con la ventana más grande
super + g
	bspc node -s biggest.window

#
# Estado y banderas
#

# Establecer el estado de la ventana
# tiled: ventanas en mosaico
# pseudo_tiled: ventana en mosaico pero puede ser redimensionada
# floating: ventana flotante
# fullscreen: ventana en pantalla completa
super + {t,shift + t,s,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# Establecer las banderas del nodo
# TODO: Atajo para sticky y eliminar el resto
super + ctrl + {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

#
# Intercambio y escritorios
#

# Mover el nodo en la dirección indicada
super + shift + {Left,Down,Up,Right}
	bspc node -s {west,south,north,east}

# Cambiar al escritorio indicado
super + {1-9,0}
	bspc desktop -f '^{1-9,10}'

# Enviar ventana al escritorio indicado
super + shift + {1-9,0}
	bspc node -d '^{1-9,10}'

#
# Mover y redimensionar
#

# Expandir una ventana moviendo uno de sus lados hacia afuera
super + alt + {Left,Down,Up,Right}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# Contraer una ventana moviendo uno de sus lados hacia adentro
super + alt + shift + {Left,Down,Up,Right}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# Mover una ventana flotante
super + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}
